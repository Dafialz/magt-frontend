# ========= Build =========
[build]
  publish = "."

# ========= Headers =========

# Довгий кеш для статичних картинок/стилів/скриптів у /assets
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Динамічні ресурси без кешу
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/partials/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"

# TonConnect manifest: CORS + короткий кеш + м’якший CORP
[[headers]]
  for = "/tonconnect-manifest.json"
  [headers.values]
    Content-Type = "application/json; charset=utf-8"
    Cache-Control = "public, max-age=60"
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Methods = "GET, OPTIONS"
    Access-Control-Allow-Headers = "*"
    Cross-Origin-Resource-Policy = "cross-origin"

# Локалізаційні JSON
[[headers]]
  for = "/i18n/*"
  [headers.values]
    Content-Type = "application/json; charset=utf-8"
    Cache-Control = "public, max-age=300"

# Базові безпекові заголовки (глобально)
[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options       = "nosniff"
    X-Frame-Options              = "SAMEORIGIN"
    Referrer-Policy              = "strict-origin-when-cross-origin"
    Permissions-Policy           = "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), usb=()"
    Strict-Transport-Security    = "max-age=31536000; includeSubDomains; preload"
    # Для TonConnect: дозволяємо попапи/редиректи гаманців
    Cross-Origin-Opener-Policy   = "same-origin-allow-popups"
    # М’якший CORP, щоб працювали мости TonConnect
    Cross-Origin-Resource-Policy = "cross-origin"

# ========= Redirects =========

# --- ВИНЯТКИ: не переписуємо статичні директорії ---
[[redirects]]
  from = "/assets/*"
  to   = "/assets/:splat"
  status = 200

[[redirects]]
  from = "/js/*"
  to   = "/js/:splat"
  status = 200

[[redirects]]
  from = "/partials/*"
  to   = "/partials/:splat"
  status = 200

[[redirects]]
  from = "/i18n/*"
  to   = "/i18n/:splat"
  status = 200

# SPA rewrite (після винятків!)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
